[build]
builder = "nixpacks"

[build.nixpacks]
setupStates = ["node"]
nodeVersion = "22"

[deploy]
startCommand = "npx vite preview --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
